[pytest]
DJANGO_SETTINGS_MODULE = config.settings
addopts = --tb=short --strict-markers
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
asyncio_mode = auto
markers =
    slow: tests that load face_recognition / dlib (optional: pytest -m "not slow")
